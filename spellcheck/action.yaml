name: 'Documentation Spell Check'
description: 'Checks spelling in documentation'
inputs:
  working-directory:
    description: 'Working directory'
    required: true
    default: '.'
runs:
  using: 'composite'
  steps:
    - run: python3 ./spellcheck.py ${{ inputs.working-directory }}
      shell: bash